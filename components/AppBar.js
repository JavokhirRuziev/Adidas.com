import React from "react";
import AppBarDrawer from "./AppBarDrawer";

function AppBar({ maxWidth, margin }) {
  return <AppBarDrawer maxWidth={maxWidth} margin={margin} />;
}

export default AppBar;
